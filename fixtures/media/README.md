# Media Fixtures

`bright-color-bars.mp4` is a tiny generated H.264/AAC MP4 for manual playback QA. Import it through the app when a visual smoke test needs bright, non-black pixels in screenshots.

The fixture contains generated color bars and silent audio. It is not production content and should not be bundled as a lesson.
